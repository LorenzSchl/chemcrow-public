from setuptools import setup

# The version is now dynamically managed by setuptools using pyproject.toml
# Ensure chemcrow/version.py still exists and is correctly parsed by setuptools.
# Minimal setup.py for compatibility or if there are complex build steps
# not covered by pyproject.toml. For this project, it seems most, if not all,
# configuration has moved to pyproject.toml.

setup()
